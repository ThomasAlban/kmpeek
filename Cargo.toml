[package]
name = "kmpeek"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.18.1", default-features = false, features = [
    "bevy_render",
    "bevy_winit",
    "bevy_pbr",
    "bevy_anti_alias",
    "bevy_post_process",
    "bevy_core_pipeline",
    "bevy_picking",
    "mesh_picking",
    "multi_threaded",
    "file_watcher",
    "tonemapping_luts",
    "ktx2",
    "zstd_rust",
    "serialize",
    "bevy_gizmos",
    "bevy_gizmos_render",
    "keyboard",
    "mouse",
    "track_location",
    # "dynamic_linking",
] }

bevy_egui = "0.39.1"
bevy_mod_outline = "0.12.1"
bevy_pkv = "0.15"
# KMPeek integrates the framework-independent core directly with its
# egui-hosted offscreen viewport; do not replace this with transform-gizmo-bevy.
transform-gizmo = "0.11"
bevy_infinite_grid = "0.18"
bevy_vector_shapes = "0.12.1"

egui_dock = { version = "0.18", features = ["serde"] }
egui_extras = { version = "0.33", default-features = false, features = ["all_loaders"] }
egui_file = "0.25"

binrw = "0.14.0"
bytemuck = "1.13.1"
byteorder = "1.4.3"
image = { version = "0.25.1", default-features = false, features = ["png"] }
num-traits = "0.2.15"
serde = { version = "1.0.163", features = ["derive"] }
serde_json = "1.0.96"
strum = "0.26.1"
strum_macros = "0.26.1"
csv = "1.3.0"
derive-new = "0.6.0"
bon = "1.2.1"
anyhow = "1.0.86"


# Enable a small amount of optimization in debug mode
[profile.dev]
opt-level = 1

# Enable high optimizations for dependencies
[profile.dev.package."*"]
opt-level = 3

[profile.release]
opt-level = 3
lto = true
strip = true

[lints.clippy]
type_complexity = "allow"
too_many_arguments = "allow"

[profile.dev.package.objc2]
debug-assertions = false
